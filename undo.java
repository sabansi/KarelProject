import acm.program.ConsoleProgram;

interface 

public class undo extends ConsoleProgram{
	public void run() {
		while(true) {
			String input = readLine();
			analize(input);
		}
	}

	private void analize(String input) {
		if(input.contains("save")) {
			String substr = input.substring(8, )
		}
	}
}
